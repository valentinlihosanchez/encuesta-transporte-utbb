[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run migrate && npm run seed:admin && node src/server.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
